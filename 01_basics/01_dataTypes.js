"use strict" //treat all the code with a newer version of JS

// aleart(3 + 3) it will give error because we are useing node.js not browser;

//datatypes
/* number => range 2 to pow 53
   bigInt =. for bigger value
   string = ""
   boolean = true/false
   undefined = not define value 
   null = standalone value
   symbol = identify uninque value 
*/
//typeOf we can use two way 

console.log(typeof (null)) // null is a object
console.log(typeof undefined) // undefined is type 